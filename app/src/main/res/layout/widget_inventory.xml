<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="12dp"
    android:background="@drawable/bg_inventory_widget">

    <!-- Logo -->
    <ImageView
        android:id="@+id/ivLogo"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:src="@drawable/ic_inventory_logo" />

    <!-- Título Inventory -->
    <TextView
        android:id="@+id/tvInventoryTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/ivLogo"
        android:layout_alignStart="@id/ivLogo"
        android:layout_marginTop="4dp"
        android:text="Inventory"
        android:textColor="#e7522e"
        android:textStyle="bold"
        android:textSize="16sp" />

    <!-- Texto ¿Cuánto tengo de inventario? arriba derecha -->
    <TextView
        android:id="@+id/tvQuestion"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_marginEnd="55dp"
        android:text="¿Cuánto tengo de inventario?"
        android:textColor="@android:color/white"
        android:textSize="15sp"
        android:textStyle="bold" />

    <!-- Ícono ojo (toggle) -->
    <ImageView
        android:id="@+id/ivEye"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_marginStart="0dp"
        android:contentDescription="@string/app_name"
        android:src="@drawable/ic_eye_open_white" />

    <!-- Saldo / asteriscos -->
    <TextView
        android:id="@+id/tvBalance"
        android:layout_width="300dp"
        android:layout_height="30dp"
        android:layout_below="@+id/ivEye"
        android:layout_alignParentEnd="true"
        android:layout_marginTop="10dp"
        android:text="$ ****"
        android:textColor="@android:color/white"
        android:textSize="20sp"
        android:ellipsize="none"
        android:singleLine="true"
        android:gravity="end"
        android:textStyle="bold" />


    <!-- Texto Gestionar inventario -->
    <TextView
        android:id="@+id/tvManage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_toStartOf="@id/ivManageIcon"
        android:layout_marginEnd="4dp"
        android:text="Gestionar inventario"
        android:textColor="@android:color/white"
        android:textSize="13sp" />

    <!-- Ícono naranja para gestionar -->
    <ImageView
        android:id="@+id/ivManageIcon"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:src="@drawable/ic_manage_orange" />

</RelativeLayout>
